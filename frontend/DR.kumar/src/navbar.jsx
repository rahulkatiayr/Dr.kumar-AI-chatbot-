import React from "react";
import "./style.css"; // Import the CSS file

export default function Navbar() {
    return (
        <nav className="navbar">
            <div className="navbar-text">Consult with AI Doctor</div>
        </nav>
    );
}
